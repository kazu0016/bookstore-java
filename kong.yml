_format_version: "1.1"

services:
  - name: bookstore-service
    url: http://bookstore-app:8080/  # <--- APONTE PARA A RAIZ (Sem o bookstore)
    routes:
      - name: bookstore-route
        paths:
          - /
        strip_path: false            # <--- MANTÉM O CAMINHO ORIGINAL
        preserve_host: true          # <--- EVITA ERRO DE DNS
